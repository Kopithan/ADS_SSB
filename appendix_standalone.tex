% ---------------------------------------------------------------------------
% Standalone build of the appendix only.
%
% Use this if you want the code as its own PDF to print and staple, instead of
% appending it to main.tex. It reproduces the same page border as the report.
% Compile with pdflatex (twice, so the table of contents settles):
%
%       pdflatex appendix_standalone
%       pdflatex appendix_standalone
%
% If instead you are adding the code to the report itself, ignore this file and
% follow the instructions at the top of appendix.tex.
% ---------------------------------------------------------------------------

\documentclass[11pt,a4paper]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[a4paper,top=2.4cm,bottom=2.6cm,left=2.4cm,right=2.4cm]{geometry}
\usepackage{listings}
\usepackage{xcolor}

% ---- page border, matching the report -------------------------------------
\usepackage{tikz}
\usetikzlibrary{calc}
\usepackage{eso-pic}
\AddToShipoutPictureBG{%
  \begin{tikzpicture}[remember picture,overlay]
    \draw[line width=0.8pt]
      ($(current page.south west)+(1.2cm,1.2cm)$) rectangle
      ($(current page.north east)+(-1.2cm,-1.2cm)$);
  \end{tikzpicture}%
}
% ---------------------------------------------------------------------------

\title{\textbf{System Serial Bus Design}\\[2mm]\large Appendix: Source Code}
\author{}
\date{}

\begin{document}

\maketitle
\thispagestyle{plain}

\input{appendix}

\end{document}
